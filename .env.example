# OANDA API credentials
OANDA_TOKEN=your-oanda-access-token
OANDA_ACCOUNT_ID=101-001-1234567-001
OANDA_ENV=practice

# Default trading parameters (can override via CLI flags)
DEFAULT_STRATEGY=smartSignals
DEFAULT_INSTRUMENT=EUR_USD
DEFAULT_GRANULARITY=H1
DEFAULT_POSITION_SIZE=0.02
